# llm_agent

This agent is a component of a Data handling process. It is for identifying if a news is related to "soccer" and if it is a good news.

It will read data from mysql database, identify based on the text content and then save the result back to the database.
